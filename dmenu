#!/bin/sh
ACT="/tmp/.nextCMD"
CMD="dmenu.bin"
if [ "`pwd`" != "/mnt/memory/dmenu" ]; then 
    CMD="dmenu.bin"
fi

touch "$ACT"

if ./$CMD; then
    if [ -f "$ACT" ]; then 
        if  ! sh $ACT; then
#           sleep 5
            echo
        fi
        rm -f "$ACT"
    fi
else
    sleep 30
fi
